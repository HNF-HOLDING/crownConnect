{
  "name": "crownconnect-aws-api",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "package": "npm ci --omit=dev && zip -qr ../dist/crownconnect-api.zip index.js package.json node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.900.0",
    "pg": "^8.16.3"
  }
}
